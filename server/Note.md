## Package manager
- npm or npx
- pnpm 
- bun
- yarn

### Package / dependency
- 3 type package
- global : when installing the nodejs in machine, it gets setup
    - npm i /install <package> -g
- dependancy : when a project needs, we install and use it
- dev : when needed in a project but only while development

